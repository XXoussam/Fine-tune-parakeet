# --- Fine-tuning (finetune_parakeet.py, an4_smoke_test_data.py, build_manifest.py / prepare_manifest.py) ---
# Needs a CUDA GPU - install these on the Linux GPU VM, not on this local machine.
#
# Install PyTorch matching the VM's CUDA version FIRST, from
# https://pytorch.org/get-started/locally/ (nemo_toolkit does not pin one for
# you), then: pip install -r requirements.txt
nemo_toolkit[asr]
lightning
hydra-core
omegaconf
soundfile

# --- Local-only (evaluate.py) -------------------------------------------------
# Not needed on the VM. evaluate.py runs on the local machine, no GPU/NeMo
# required - just this for its WER-distribution / WER-vs-duration plots.
matplotlib

# --- Local-only smoke test (try_deberta-v3-base.py) ---------------------------
# Not needed on the VM (or for fine-tuning at all) - only if you run this
# standalone script locally.
# transformers
# certifi
